devotional.backfill.enabled=false
devotional.backfill.horizon-days=14
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:koino_db}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:secretpassword}
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.mail.host=${MAIL_HOST:localhost}
spring.mail.port=${MAIL_PORT:1025}
spring.mail.username=${MAIL_USERNAME:test@koino.local}
spring.mail.password=${MAIL_PASSWORD:test-password}
app.mail.from=${MAIL_USERNAME:test@koino.local}
security.jwt.secret=a29pbm8tZGV2ZWxvcG1lbnQtand0LXNlY3JldC1jaGFuZ2UtbWUtMjAyNg==
security.jwt.expiration=PT24H
security.password-reset.expiration=PT30M
security.email-verification.expiration=PT24H
app.frontend-url=http://localhost:5173
app.google.client-id=test-client
app.cors.allowed-origins=http://localhost:5173
battle.questions.backup-path=data/battle-question-catalog.generated.json
battle.questions.bootstrap-path=data/battle-question-catalog.generated.json
battle.questions.gemini.enabled=false
bible.licensed-import.enabled=false
spring.security.oauth2.client.registration.google.client-id=${CLIENT_ID:test-client}
spring.security.oauth2.client.registration.google.client-secret=${CLIENT_SECRET:test-secret}
spring.security.oauth2.client.registration.google.scope=openid,email,profile
